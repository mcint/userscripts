# drafts/

Work-in-progress scripts. May be broken, partial, or spikes. No header
discipline required here — this is the workbench.

Promote to [`../scripts/`](../scripts/) once it works and has a complete
`==UserScript==` header with a semver `@version`.
